package org.acme.quickstart;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativePocResourceIT extends PocResourceTest {

    // Execute the same tests but in native mode.
}